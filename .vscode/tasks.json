{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build timedaemon",
            "type": "shell",
            "command": "clang++",
            "args": [
                "-Wall",
                "${workspaceFolder}/source/lightsdaemon.cpp",
                "-o",
                "${workspaceFolder}/bin/lightsdaemon"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}